<p style="color: green"><%= notice %></p>

<h1 class="is-size-1">Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <%= render "items/item", item: item %>
  <% end %>
</div>

<%= link_to "New item", new_item_path %>
